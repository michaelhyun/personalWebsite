# OS / tooling
.DS_Store
node_modules/
*.log

# Local build tooling only (sharp, for generating images) — keep the deploy static
package.json
package-lock.json

# Private — never publish
business_plan.md
.claude/

# Unused duplicate asset (clean copy is michael-portrait.jpg)
assets/img/portrait2 (1).jpg
